# Crew technical debt ledger

Every compromise, recorded in the same commit that creates it (CLAUDE.md rule 9; Appendix C).
Format: `- <date> · <task> · <what was compromised> · <why> · <how it gets repaid>`

## Open

- 2026-09-17 · W2 / CI · **TestFlight cannot archive: the team has hit Apple's certificate cap, because every archive on a fresh GitHub runner mints a new Apple Development certificate** · run 35294010935: "Your account has reached the maximum number of certificates … No profiles for 'com.maxwellcuenca.crew' … iOS App Development". `testflight.yml` archives with automatic signing and `-allowProvisioningUpdates`; a runner holds no private key from the last build, so Xcode creates a development certificate per run — one per TestFlight build since 2026-09-08 — until Apple refuses. Nothing in W2 touched signing; build 126 shipped through the same workflow on 2026-09-16 · repaid in two halves: (1) OWNER, now — revoke the runner-minted "Apple Development" certificates at developer.apple.com (all of them; no Mac exists that uses one; the "Apple Distribution" ones stay) and re-dispatch testflight; (2) BUILDER, owner-approved first — an archive that mints nothing: sign the archive with the cloud-managed distribution identity (or archive unsigned and let `-exportArchive` sign) so a fresh runner never asks for a development certificate. Until (2) lands, the cap returns after roughly as many builds as it took to hit it. **UPDATE 2026-09-18 — (2) SHIPPED, pending its first run:** the owner's dispatch (run 35304691405, build 143) failed on the same line, so `testflight.yml` now archives with `CODE_SIGN_IDENTITY="Apple Distribution"` — the cloud-managed identity the export step has used on every successful upload, whose key Apple holds and reuses — with an UNSIGNED-archive fallback in the same step (the export then signs). No step asks for a development certificate any more. (1) is still worth doing once, to clear the dead runner-minted certificates from the account, but no longer gates a build. **REPAID 2026-09-18, run 35305232232 (build 145 uploaded).** The first shape (archive signed with the distribution identity) was refused under automatic signing — "conflicting provisioning settings" — so the final shape is: the archive is built UNSIGNED (`CODE_SIGNING_ALLOWED=NO`, nothing minted), a step writes `teamID` into `ExportOptions.plist` from the `APPLE_TEAM_ID` secret (an unsigned archive records no team — "No Team Found in Archive" otherwise), and the export signs with the cloud-managed Apple Distribution identity and uploads. No step in the workflow asks for a development certificate any more; the owner's revocation of the dead ones is housekeeping. Two automatic runs (CI for 4bd9aa5 and 6abd914) will upload behind this one; `manageAppVersionAndBuildNumber` lets Xcode bump a build number App Store Connect has already seen, so TestFlight may list 145 and one or two higher numbers with the same code.
- 2026-09-17 · W2 / 8.4 · **an INTERMITTENT launch hang of a seeded returning user on the CI simulator, 2 of 8 launches** · #39 `HomeStatesTests.testAllDone…` (110.7 s) and #40 attempt 1 `testRestDay…` (112.5 s): the app launched, never answered an accessibility snapshot (`Requesting snapshot … pid 28918` at t = 46.7 s, no reply), the assertion's screen dump was EMPTY and XCTest reported "Restarting after unexpected exit, crash, or test timeout". Both hung launches had the member's single training day on the app's TOMORROW, but a third launch with that seed rendered, and every "tomorrow" path is bounded (`PlanRotation.nextTrainingDayKey` ≤ 7, `NextUpLine.kindOn` ≤ 7, `ServerHydrate.pullIfEmptyBounded` capped) — so it is not a loop this reading can name. The same class earlier lost a screenshot to a "wedged simulator" (2026-09-16, F48). Attempt 2 of #40 was green · repaid by EVIDENCE, not a rerun: on the next occurrence download the run's `ios-test-results` xcresult and read the failure's attachments (the screenshot tells skeleton from blank from frozen), and consider a launch-time `os_signpost` around `pullIfEmptyBounded` + `HomeModel.refresh` so a hang names its phase. A rerun is how it was shipped today; it is recorded here so a green run is not mistaken for a fix.

- ~~2026-09-16 · A20.11 / F49 · the birth-year field accepts a number its own error message says it will not~~ · **REPAID by F50, in its own commit as ruled** · both clients now mirror the server's two bounds from the generated constants — `>= SpecConstants.birthYearMin` and `getUTCFullYear() - birthYear >= SpecConstants.minimumAgeYears` — and `web/tests/onboarding-birth-year.test.ts` pins the PARITY rather than the wording, sweeping every year from before the floor to past today and asserting the client accepts exactly the set `requireSignupGates` accepts. Measured before the fix: **the old validators each disagreed with the server on 16 years** (1898-1899 and 2014-2027), and iOS additionally accepted `"19"`. No new user-facing copy was invented — both sentences were already approved and already on the screen. iOS takes the year from `DayKey` in **UTC**, not the local calendar, because a local year running BEHIND UTC would make the client stricter than the server and refuse someone who just turned 13.
- ~~2026-09-16 · A20.11 / F48 · a screenshot timeout carried undiagnosed into the next run~~ · **RESOLVED by run 35073421853** · `HomeStatesTests.testAllDoneReportsTheDayAndAsksForNothing` passed (4 tests, 0 failures) once the three keyboard-focus failures it shared run 35069768536 with were fixed, which settles the question the entry was opened to answer: it was **collateral from a wedged simulator, not a real hang.** `shoot()` stays fatal, and the judgement to fix the understood causes first rather than weaken the probe was the right one.
- 2026-09-16 · A20.11 / F48 · **SEVEN TEST FILES CHANGED TO FIX TWO FAILURE CLASSES, AND NONE OF IT CAN RUN ON THIS MACHINE** · the `hasKeyboardFocus` wait added to `typeInto` and the five `workouts.first { $0.kind == "push" }` pins are WRITTEN-UNVERIFIED; `swift-xref` and `doctrine-lint` pass but neither compiles XCTest nor drives a simulator · no Mac, no Xcode — and the `ios` job had not executed a single test since `334ad67` (2026-09-10), so A18, A19 and A20 Build A ALL landed without this suite ever running, which is precisely why two never-before-exercised defects surfaced together · repaid by the macOS CI job on the F48 run. **The 5 failures in 35069768536 were 100% test defects and 0% app defects — the app compiled clean with 0 errors, and that is the first real evidence A18/A19/A20 are sound.**
- 2026-09-15 · A20 / F45 · **THE COMMIT QUEUE'S OWN ORDERING RULE PUT HALF OF BUILD B INSIDE BUILD A, AND IT SHIPPED A RED MASTER (run 34918410611)** · this file's header states the rule in writing — *"a block stages every listed path that has changes, so a later EDIT to a file lands in the FIRST block that names it"* — and F45 was written naming `HomeScreen.swift`, `HomeModel.swift` and `HomeStatesTests.swift`, which Build B then rewrote before the queue was ever run. So F45 committed Build B's `HomeScreen` (referencing `EmberTokens.Typography`, `LogRowList`, `StateBlock`, `NextUpRow`) while the files DEFINING them stayed in the held F46 block. swift-xref caught 2 findings on the runner and would have caught them locally too — but locally those symbols all existed, because the working tree held BOTH builds at once. **The real lesson is not the ordering rule, it is that a local gate run against a working tree containing un-queued work does not test what the commit contains.** · repaid by F47, which restores the three files to Build-A-only content after snapshotting Build B to the scratchpad; the tree was reverted to HEAD-plus-Build-A before re-running every gate, so the gates finally measured the commit. **For the next multi-build pass: no two queue blocks may name the same path, or the later build's content must not exist on disk when the earlier block runs.**
- 2026-09-12 · A20 · **the whole Build B redesign is WRITTEN-UNVERIFIED, and it is the largest un-compiled Home change yet** · `LogRow.swift`, `HomeModel+Rows.swift`, the rewritten `HomeScreen`, `HomeHeader` and `TodayCard`, and Home's first `.crewBottomBar` adoption have never rendered on a simulator or a device from this machine · no Mac, no Xcode; `swift-xref` label-checks call shapes and `doctrine-lint` checks structure, and neither compiles SwiftUI — the last two reds on master were both invisible to them (a scope resolution and an `[Int]`→`Set<Int>` conversion) · repaid by the macOS CI job, which now photographs six Home states including two in the dark appearance. **Look at the artifact before trusting the green.**
- 2026-09-12 · A20.12 · **the type scale exists but only Home is built against it** · the other four tabs still choose fonts per file, so the repo now has a token nobody outside Home obeys — which is a worse state than none if it is left · repaid by a mechanical sweep per screen; recorded now because a half-adopted token reads as a system and is not one. `token-parity.test.ts` guards the token's SHAPE, not its adoption.
- 2026-09-12 · A20.4 · **`WeeklyRing` is now used by exactly one screen (Progress)** · Home rendered it and no longer does; the component and its geometry stay because Progress's week card is a different surface with a different question, and deleting it would be a Progress change riding inside a Home pass · repaid if Progress ever drops it too, at which point the file goes with it.
- ~~2026-09-12 · A20 / T-A3 · the fix for the three `Birth year` UI-test failures is a HYPOTHESIS, not a diagnosis~~ · **RESOLVED by run 35073421853, and the hypothesis was RIGHT** · the entry said "if they stay red the next move is to read the xcresult rather than guess again" — the xcresult was read, and its trace is the proof: the first tap on `Password` never took focus (`hasKeyboardFocus` polled 33.3 s → 37.3 s and never went true), the retry tap at t=37.59 logged `Scroll element to visible` and focus arrived at t=40.17, after which every field typed cleanly. **So the race was real, the field was genuinely obscured, and A19's animating bottom edge is why** — but the guard A20.9 wrote only covered the numeric keyboard, so it fixed `Birth year` and left `Password`, one field earlier. Repaid by F48's general `hasKeyboardFocus` wait. No further guessing was stacked on it.
- 2026-09-12 · A20.10 · **the pause arrives one foreground late when it was created elsewhere** · `SyncDriver.drainThenPullPause()` pulls it after the queue drains, while `HomeModel.refresh()` runs on the same `scenePhase → .active` — so a pause created on the web shows on Home at the NEXT foreground, not this one. The seeded case (an empty Store, which is what `HomeStatesTests` and a reinstall both produce) is correct immediately, because `ServerHydrate.pullIfEmpty` runs before Home judges today · repaid by having the pull re-ask Home to refresh; not done here because the only concrete route is a notification or a callback, and inventing either for one caller is the abstraction expansion C1 forbids. Revisit when a second caller needs it.
- 2026-09-12 · A20 / T-A1 · **A18.4's UNPOSTED rest-day premise line is asserted nowhere** · `HomeStatesTests.launchHome` must post a meal to clear the bridge (§1D keeps it until the first post EXISTS), so every state that file seeds has `posted == true` and only the "Today counts." branch is reachable from a UI test. The previous assertion demanded the unposted string and had therefore never passed · repaid by T-D1: A20's rest STATE BLOCK carries the premise unconditionally, so one assertion covers both branches. Until that lands, the copy rule A18.4 exists for is unguarded.
- 2026-09-11 · A19.1 · **Home and the workout editor have NOT adopted `.crewBottomBar`**, so the two screens with the worst anchoring are still on the old mechanism · Home because A19.1 itself ratifies "Home last … let A18's tests land first" and A18's iOS half has never compiled on a Mac; the editor because its primary lives in the nav bar, which makes moving it R11 (Stage E, after A15 in the ratified ordering) · repaid by adopting on both after the first green CI run that compiles A18 and A19 Stages A/B/D. Until then `home-layout.spec.ts`'s gap gate stays a 25%-of-viewport ratio rather than an absolute number.
- 2026-09-11 · A19 · every A19 change so far is WRITTEN-UNVERIFIED: `safeAreaInset`, `ToolbarItemGroup(placement: .keyboard)`, the re-parented `ScrollView`s on the onboarding screens and the session's re-parented bottom group have never rendered on a simulator or a device from this machine · no Mac, no Xcode; swift-xref checks call shapes and doctrine-lint checks structure, and neither compiles SwiftUI · repaid by the macOS CI job and then the owner's phone. The onboarding screens are the ones to look at first: they gained a GeometryReader + ScrollView + minHeight sandwich that is easy to get subtly wrong (a Spacer with no floor simply collapses).
- 2026-09-11 · A19.5 / R5 · "Discard" moved to the nav bar as a `topBarTrailing` item rendered in INK rather than in the danger colour · Part III law ① makes every control ink and the confirmation dialog already carries `role: .destructive`, so the weight sits in the dialog rather than in the bar · recorded because a reviewer will reasonably ask why a destructive action is not red, and because 6.3's rule was about ADJACENCY, which is what actually moved.

- 2026-09-10 · A18 / J032 · **Home's bottom anchor is a `Spacer` INSIDE scrolling content, which is not an anchor** · `HomeScreen.swift`'s `Spacer(minLength: 0)` sits in a `ScrollView` under `.frame(minHeight: proxy.size.height)`, so it collapses to zero the moment content exceeds the viewport — measured by the new layout gate: at 375×667 the rest-day Home already overflows (document 768 px) and the gap is 24 px, while at 440×956 it is 212 px. A17.2's guarantee that "the day's single ink-filled primary sits in the thumb zone on every state" is therefore true only while Home FITS, and every element A18 added (two captions, the next-up block, three full-width rows, the paused control, the all-done summary, a banner that now renders) moves the collapse onto more devices and more states. At accessibility-XXL on an SE it is already false · repaid by moving the primary out of the scroll — `safeAreaInset(edge: .bottom)` on iOS, `position: sticky` on web — which is a MECHANISM change to a ratified clause and needs its own owner decision, not a quiet edit inside a Home pass. When it lands, `home-layout.spec.ts`'s gap gate becomes an absolute number instead of a 25%-of-viewport ratio.
- 2026-09-10 · A18 / J032 · the layout gate's gap threshold is a RATIO (25% of the viewport) rather than a design-system value · the first version was absolute (4 × sectionGap = 96 px) and went red at 108 px on 393×852 against a layout behaving exactly as ratified — while the primary is bottom-anchored inside the scroll, slack IS viewport minus content, so a taller phone necessarily has more of it · repaid by the entry above: once the primary leaves the scroll there is no interior gap to measure and the number can be absolute. The second clause (a gap may not exceed the group it introduces) is content-relative and stays either way.
- 2026-09-10 · A18.6a · `weekMarks` consults only the ACTIVE pause, the one `Store.activePause` / the `pauses` query returns for today · a pause that STARTED and ENDED earlier in the same ISO week still leaves its days rendering as `missed`, because neither engine fetches pauses by overlap with the week · repaid by fetching the week's overlapping pauses rather than today's; recorded rather than fixed because the state the owner will see is the live one, and widening the query on both engines is a bigger change than the defect it closes.
- 2026-09-10 · A18.12 · only HOME's five states are now reachability-tested; Plan, Crew and Progress still assert a five-element literal · `CrewScreen` genuinely reaches `.offline`, but nothing in the app assigns Plan's or Progress's · repaid by giving each its own `LoadState.of(...)` the way `HomeLoadState` now has one — a per-screen pass, not a Home one. `ShellStatesTests.testTheOtherShellsDeclareFiveStates` says so in its name so the coverage is not overstated.

- 2026-09-10 · A18.13 · **OPEN OWNER DECISION, not a compromise in code** · Crew's streak is DAILY while its plan is WEEKLY, so a rest day has to be bought with an unrelated post — which is the root cause of the owner's "why does the rest day card have a Post a meal CTA". The market went the other way: Hevy ("rest days do not break your weekly streak"), Nike Run Club (consecutive weeks). A ~60,000-post study (British Journal of Health Psychology, UCL + Loughborough) reports streak users describing "guilt, irritation and shame" and "greater discouragement over time", which is the failure mode spec:452 exists to prevent. The owner ruled: **keep daily, log the evidence**. Recorded here so the next session does not re-derive the argument. **A18.4's premise line ("Rest days count too — …") exists ONLY because the streak is daily; if the cadence ever moves to weeks, that line is deleted in the same commit or the registry will carry copy that contradicts the engine.**
- 2026-09-10 · A18 / J029 · the iOS half of this pass is WRITTEN-UNVERIFIED, as every Home file before it was: the full-width log rows, the two header captions, the next-up block, the paused control and the bridge's absorbed resume have never rendered on hardware or in a simulator from this machine · no Mac, no Xcode, and the 8.9 snapshot matrix still does not exist · repaid by the macOS CI job (which now captures the four states as artifacts, J034) and then by the owner's phone.
- 2026-09-10 · A18.7 · a bonus workout completed on a NON-training day now shows no mark on the week strip on either engine (web previously showed `done`, iOS showed nothing; iOS's rule won so the strip can be read against the ring) · the alternative was a seventh mark state for "extra", which answers a question nobody asked and costs a mark, a colour decision and a sentence clause · repaid if the owner asks for it; the bonus is already visible in the journal and on the Progress heat map, so nothing is lost, only un-summarised.

- 2026-09-09 · A11 · the session screen swipe is hand-written (SwipeToRemove) rather than SwiftUI's .swipeActions · that modifier only works inside a List and the session screen is a ScrollView of Cards, where it compiles, renders nothing and fails silently · never repaid as such; recorded so nobody "simplifies" it back to .swipeActions. It is WRITTEN-UNVERIFIED: the DragGesture has never run.
- 2026-09-09 · A11 · the "Set removed · Undo" row has no timer — it stays until the next removal or the end of the workout · same deliberate omission as the plan editor's Undo (A4): a row that vanishes on a clock is a row you cannot get back · repaid only if the owner asks for the timer.
- 2026-09-09 · A10 · the weight tape ships on iOS only; the web twin gets a number input beside the same ± pair · a drag-ruler is a phone gesture and a keyboard types faster — the plan's parity rule is data-and-engine, affordances may diverge · never repaid as such; recorded so the difference is deliberate rather than drift.
- 2026-09-09 · A10 · the tape is WRITTEN-UNVERIFIED: .scrollTargetLayout / .scrollPosition / .scrollTargetBehavior(.viewAligned) and the accessibilityAdjustableAction have never run — no Mac here, and swift-xref cannot type-check SwiftUI modifiers · repaid by the macOS CI job compiling it and by the 8.9 snapshot matrix at SE/XXL; the VoiceOver swipe-to-adjust needs a device pass.
- 2026-09-09 · A9 · the legacy `units` field is still written on the user document as a mirror of `weightUnit`, and the two new fields are DERIVED on read for accounts written before the split (no bulk migration) · TestFlight build 3 is installed on the owner phone and reads `units`; removing it would break that build mid-beta · repaid when no build older than the A9 release is in use: drop `units` from UserDoc, PublicUser and UpdateMeInput, and delete weightUnitOf/distanceUnitOf.
- 2026-09-09 · A9 · a set logged BEFORE this change has no `weightUnit` of its own, so it falls back to the account's current weight unit at comparison time — which is correct only while that account has not switched units since logging it · no account has switched (the field was inert until now) · repaid by a one-off backfill stamping every existing set log with its owner's unit, if the cohort ever grows before that is done.
- 2026-09-09 · A9 · MeasurementSystemHint lives in ios/Crew/Api/, not Engine/, because Locale.measurementSystem is a Darwin API and Engine/ must compile on Linux for the twin-engine vector run · a platform lookup is not engine logic · never repaid; recorded so nobody "tidies" it into Engine/ and breaks the Linux build (it did break once, caught by docker swift test).
- 2026-09-09 · A9/review · shipped defect, now scheduled: `units` is a display suffix with NO conversion code anywhere in the monorepo, so switching kg/lb in Settings relabels every stored weight (a 135 lb bench reads "135 kg"), switches PlateMath from a 45 lb bar to a 20 kg bar and prescribes physically different plates, and corrupts PR detection (a false "new best" going kg→lb, permanent blindness going lb→kg) · nobody had switched units, so nothing surfaced it · repaid by A9 stage 1 (per-set unit tagging + normalised comparison + vectors V52/V53).
- 2026-09-09 · A16/review · pre-existing accessibility defect: ember #FF6600 on canvas #FAF8F5 measures 2.77:1, below WCAG 1.4.11's 3:1 for a graphical object · Part III fixed the hue before the gate was checked · repaid by a same-hue light-mode value (#DF5908 3.56:1 or #D45313 3.93:1) or by compounding the flame with an ink stroke. It becomes conspicuous the moment the A16 macro tokens ship, since all three of those pass.
- 2026-09-09 · A11/review · pre-existing wire shape: `SetLogDTO` carries no id and no order, so a set's POSITION is its identity across the sync boundary · sets were append-only until A11 · repaid before set removal ships, or removal races a concurrent patch and deletes the wrong row.
- 2026-09-09 · review · no vector on either engine covers units, weight, plate math or distance — `shared/vectors/` holds seven files and none reference them, so both engines are structurally blind to the subsystem A9/A10 change · the subsystem was display-only until now · repaid by V52/V53 in stage 1.
- 2026-09-09 · A2/review · a standalone cardio log writes a post of `type: "workout"` (web sessions.ts, iOS SessionActions), so every count of "workouts" silently includes walks · A2 lifted the cardio ban without giving cardio its own post type · repaid in stage 6 (A14) when the three vectors get distinct journal rows.
- 2026-09-09 · review · three contrast failures against the 6.5 gate on the Session screen: skipped exercise names at 2.53:1 (missedGray on white at 17 pt semibold), ghost rows applying a blanket .opacity(0.5) with no Reduce Transparency or Increase Contrast check, and the ± controls pinned at a fixed 44 pt with no @ScaledMetric so they never grow at accessibility sizes · no device or Xcode a11y audit has ever run (no Mac) · repaid in stage 2 (W020, W023).

- 2026-09-09 · A1 · no SwiftData migration is written for the plan schema (LocalPlan.trainingWeekdays, LocalWorkoutTemplate.order replacing weekday, LocalSession.workoutKind, LocalSetLog.distanceMeters, LocalPost.summary) · one beta phone exists and it reinstalls the build · repaid by a VersionedSchema + migration plan before any build reaches a tester who must keep data. · UPDATE 2026-09-09 evening: TestFlight build 3 installed over build 2 crashed at launch on the container's fatalError; the Store now removes an unopenable on-disk store (and its -shm/-wal) and starts over — the server re-hydrates it (ServerHydrate) — so a schema change costs the phone its unsynced local rows, never the app. A real migration (VersionedSchema + SchemaMigrationPlan) is still owed before the public release.
- 2026-09-09 · A1 · the Full-Body A/B templates and `split.fullBodyMaxTrainingDays` stay in the seed but nothing generates them any more · the owner asked for PPL only; deleting seed data the checker validates is a separate owner call · repaid by an owner decision to delete them (check-seeds and the `WorkoutKind` type shrink with them) or to bring full-body back at ≤ 2 days.
- 2026-09-09 · A1 · the weekly ring marks a training day done for ANY completed non-cardio session that day, not only rotation kinds (web today-state.ts and iOS HomeModel alike) · a legacy-named session on a rebuilt plan is rare and the simpler rule reads right · repaid if a tester ever sees "Start workout" again on a day they trained.
- 2026-09-09 · A2 · the cardio row has no running timer — minutes are typed (stepper), never measured · the plan parks the timer (§5) · repaid by an elapsed-time control inside CardioRow that writes the same field.
- 2026-09-09 · A2 · `logCardio` (like the older `quickComplete`) enqueues createSession and patchSession with the same `now`; FIFO order between the two rests on SwiftData's tie order · pre-existing pattern · repaid by a sequence column on OpRecord.
- 2026-09-09 · A3 · a post op the server rejects for good (400, held as poison) suppresses the gamification reconcile until the user resolves it at the 24 h choice · A3 as written ("pending, in flight or held"); the phone keeps its optimistic streak until Retry / Post without photo / Delete · repaid if the 24 h choice ever proves too late — surface the held post earlier.
- 2026-09-09 · A3 · the iOS bonus sheet dismisses itself and pushes SessionScreen in the same tap · untested on a device, a dismiss/push race is possible · repaid on the device pass (push after `onDismiss` if it flickers).
- 2026-09-09 · A4 · the editor's "Removed {name} · Undo" row has no 5 s timer on either platform — it stays until the next edit, Save or Cancel · no constant names five seconds and the persistent row reads calmer · repaid only if the owner wants the timer (one constant, two setTimeout/Task lines).
- 2026-09-09 · A4 · the rebuild reveal projects the week with an empty history (a workout completed earlier this week shows as planned, not ✓); the editor hides the back button so swipe-back is disabled, not intercepted; rebuild still starts from the default days rather than the user's current days · pre-existing shapes of the onboarding flow · repaid by passing the completed-by-day map into the rebuild projection and a Cancel that intercepts the gesture.
- 2026-09-09 · A5 · report from the stream sends the fixed reason "Reported from the crew stream" (no reason picker); the block confirmation line stays until the Crew tab disappears; blocked users still count in the pulse and the member strip (server `memberDots`/`pulseFor` do not consult `blockedIdsFor`) · A5 asks for one long-press; the strip rule is a product call · repaid by a reason picker if moderation needs it and by an owner decision on the strip.
- 2026-09-09 · A6 · the web Journal has no "Sending ↻" chip (a web post is created server-side in one request, so an undelivered post cannot exist there) · deliberate platform difference (E19 is the phone's) · never repaid; recorded so nobody hunts for it.
- 2026-09-09 · A6 · a hydrated workout post (no `sessionClientId`) joins its session by the nearest completed session on its dayKey when the server line is missing; `ExerciseChartView` still says "Last N · today N" for the latest session whatever its date; strength trends include done warm-up sets while the PR badge excludes them · the server `summary` is the truth and the chart copy predates the plan · repaid by a `sessionServerId` on LocalPost, a "latest" label, and a warm-up filter once the owner ratifies the chart change.
- 2026-09-09 · A7 · `/privacy` and `/terms` are static placeholder pages marked "Draft — replaced by the owner before submission"; the iOS rows open them from `Api.baseURL` · the owner has not drafted the legal text (OWNER-REVIEW T047) · repaid when the owner replaces the two page bodies.
- 2026-09-09 · A7 · web Settings has no "Report something" catch-all, no "Community rules" page and no "Contact us" row (r-settings §3.2 rows 12, 13, 16); the profile photo uploads the full JPEG (the server downscales) · plan §2.5 scoped Settings to what the spec names; App Store 1.2 wants a report path (the contextual Report on a post exists) and published contact info · repaid when the owner ratifies those rows before T047.
- 2026-09-09 · T033 · `notification-facts.ts` still pads the clock and picks the median with `SpecConstants.initialsMaxLetters` (a semantically unrelated constant, brief u-settings D11) · untouched this pass · repaid by a named `clockDigits`/`medianDivisor` constant.

- 2026-09-04 · T039 · Lighthouse CI budgets are not enforced on web PRs · @lhci/cli is not in the approved dependency list · repaid by adding it to ci.yml (web-e2e job) once the owner approves the dependency; the 8.8 budget numbers are already in spec-constants (performanceBudgets).
- 2026-09-04 · T043 · the automated a11y pass on web is a hand-rolled Playwright audit (landmarks, h1, alt, names, labels, overflow) rather than axe-core · @axe-core/playwright is not in the approved list · repaid by adding it beside the substitute once approved; the substitute stays as the floor.
- 2026-09-04 · T042 · the iOS welcome-back acknowledgement (PATCH users/me) is not queued when offline · the 5.6.3 OpKind list has no profile op and the map may not grow without a plan note · repaid by an owner-approved map change (OpKind.patchMe) or by accepting that the web may ask once more.
- 2026-09-05 · T035 · the XCUITest journey ② launches with `-seededReturningUser` but nothing seeded that user (a signed-in member with a plan, a crew and a reacting crew-mate) · the seed needs a server the test can reach · UPDATE 2026-09-06: WRITTEN — UNVERIFIED — `CrewUITests/SeedClient.swift` builds the member, plan, first post, crew and crew-mate through the real API on the local dev server, hands the session over in CREW_SEED_SESSION and reacts mid-test; the app hydrates as a reinstalled phone would (R-046) · repaid when journey ② runs green on a simulator (the Mac pass).
- 2026-09-06 · T035 · `Info.plist` carries `NSAllowsLocalNetworking` so the simulator reaches the `http://localhost:3000` dev server (journeys ① and ②) · XcodeGen renders one Info.plist for every configuration here · repaid by a Debug-only override on the Mac pass; production talks https only, so the key changes nothing there.
- 2026-09-08 · T013/T024–T041 · Xcode 26.6 warns in eight feature models (Crew, Home, Onboarding, Plan, Post, Progress, Session, Settings) that a main-actor `shared` singleton is read from a nonisolated context (a stored-property default like `private let store = Store.shared`) — "this is an error in the Swift 6 language mode" · the project builds in Swift 5 mode (project.yml SWIFT_VERSION 5.10, SWIFT_STRICT_CONCURRENCY minimal), so it is a warning today · repaid by marking those models `@MainActor` (they are only ever driven by views) once the app compiles end to end; not done mid-loop so each CI run changes one thing.
- 2026-09-08 · T046 · the beta deploys on Vercel Hobby, whose cron runs at most once a day, so `web/vercel.json` schedules `/api/cron/notifications` at `0 12 * * *` instead of every minute · Hobby refuses to deploy a more frequent schedule ("Hobby accounts are limited to daily cron jobs"), and Part IV's inventory admits no other scheduler without a logged decision; `reminderDue` is minute-exact, so reminders and streak-risk nudges effectively never fire in the beta · repaid by Vercel Pro and the one-line return to `* * * * *` — the route, the secret and the rules are unchanged and unit-tested.
- 2026-09-08 · T046 · Atlas is a free M0 cluster (`Crew2`) with the network access list open to `0.0.0.0/0` and no backups · Vercel Hobby functions have no fixed egress address to allowlist, and M0 has no continuous backup · repaid at launch by M10+ with backups and alerts and a real allowlist (Vercel static IPs on Pro, or a private endpoint) — OWNER-REVIEW §5 step 5.
- 2026-09-08 · T046 · email goes out as `Crew <onboarding@resend.dev>`, which Resend delivers only to the Resend account's own address · no domain is verified for Crew yet · repaid by verifying a domain at resend.com/domains and pointing `RESEND_FROM` at it; `MODERATION_INBOX` must be that same account address until then.
- 2026-09-08 · T045 · the Associated Domains entitlement still names the placeholder `applinks:crew.example`, and the web serves no `apple-app-site-association` · universal links for invites need the Team ID and bundle id in an AASA file the web serves, neither of which existed before the accounts did · repaid by `applinks:$(CREW_API_HOST)` in `ios/project.yml` plus the AASA route once the Team ID is known; until then an invite link opens the web join page, whose "Get the iPhone app" button and the app's [I have an invite] path carry the flow.

## Repaid
- 2026-09-10 · Part III / H011 → A18.11 · **Repaid** · `hairline` #E9E4DD measured **1.26:1** on a card and 1.19:1 on the canvas, and `secondaryButtonOutline` carried the identical value — so the visible boundary of EVERY outline control in the app was effectively invisible against 6.5's 3:1 component gate, and the A17 pass worked around it per-site by dropping the vector slot's card fill. The owner then looked at those slots and called them "three strange divs", which is what an invisible affordance looks like from the outside. Repaid properly: a distinct `controlOutline` token (#938C83 light / #726A61 dark) through `shared/design-tokens.json`, applied to SecondaryButton and the Home log rows on both engines, the `contrast.test.ts` exemption deleted and replaced by a SOURCE GUARD that fails if any control boundary reaches for the hairline family again. `hairline` keeps #E9E4DD for card edges and dividers — a boundary between two surfaces is not a component, and 1.4.11 does not govern it.

- 2026-09-08 · T008 · CI ran on Codemagic (`codemagic.yaml`) as well as GitHub Actions — two files describing the same macOS job · the owner's GitHub account was locked for billing that morning (a $14.86 past-due charge on an expired card; re-saving the card paid it and Actions started within minutes, run 34224403865 attempt 3) · repaid the same evening (cold-start audit, R-055): Actions had been green on all five jobs (run 34252964640), Codemagic had never run, and the file also carried the owner's email address in a public repo — `codemagic.yaml` deleted; it stays in git history under `36b1b34` should GitHub lock again.

- 2026-09-08 · T045/T046 · the Playwright harness (`web/tests/e2e/dev-server.mjs`) inherited whatever `web/.env` held, and once the owner's beta keys existed there, `next dev` under Playwright would have mailed through Resend and uploaded to the real Blob store · the harness pinned only the variables the tests needed and `next dev` fills the rest from `.env` · repaid in the audit (R-055): every vendor variable is pinned to "" in the harness, so each lib stays on its substitute; the stray `ios/.env` (a duplicate of `web/.env` that nothing read) was deleted.

- 2026-09-08 · T008 · the S38 simulator fix (ci.yml, codemagic.yaml, five docs) is in history under OLDER commit messages (the first block naming each file swept it up; S38 itself reported "already committed") · the queue stages by path, so an edit to a committed file belongs to whichever block lists it first · repaid the same evening (F07): `commit_task` skips any block whose message is already in `git log`, so committed blocks stage nothing and only never-committed blocks claim a file, and the HISTORY section now ends the script with `exit 0` — 13 of its 62 blocks never produced a commit (their files had been swept into earlier blocks) and would otherwise still stage a future edit to any ledger they name; the FIX QUEUE section stays as the ordering rule among live blocks. The mis-attributed commits of 2026-09-08 are not rewritten.

- 2026-09-04 · T003 · V35 used the opaque placeholder id `first-post` and no vector awarded an achievement · the seed list did not exist until T006 · repaid 2026-09-04 by the awarding pass + vectors V45–V50 (kind `achievements`, README contract, both engines); V35 itself is untouched (append-only) — its placeholder id is simply an id the pass never awards, which is exactly what V35 asserts (survival, not awarding). Ratification R-037.

- 2026-09-04 · T002 · EmberColors.swift and ember.css emitted the LIGHT value for `missedGray` and `emberText` in dark mode (Part III defined no dark values; gap G4) · repaid the same day: owner set missedGray dark = #5E574F and emberText dark = #FF7A1F (Decision Registry G4), tokens updated, files regenerated, the generator no longer tolerates a missing dark value.

- 2026-09-09 · T022 · Open · the signup password field is `.textContentType(.password)`, not `.newPassword`: a signed build answers a `.newPassword` field with iOS's Automatic Strong Password, which replaced the typed text on the simulator and left one character (run 34373681818) — so the MVP forgoes the strong-password suggestion at signup; the Keychain still offers to save the pair and autofills it at login (S05). Revisit with a device run: if the suggestion behaves on hardware, restore `.newPassword` and teach the journeys to choose their own password.

- 2026-09-10 · A16.c · Open (owner) · the 18+ gate on the nutrition-target surface is specified against an age the account does not reliably hold: signup stores a birth YEAR, not a birthdate (`birthYearMin`; `requireSignupGates` compares `currentYear − birthYear`), and `birthYear` is OPTIONAL on the Sign in with Apple path — `LoginScreen` passes `nil` — so an account can exist with no age on file at all. Conservative reading adopted and recorded in Appendix A (2026-09-10): age = `currentYear − birthYear`, and an ABSENT birth year reads as under 18, hiding the surface; nothing is asked and no existing account is re-prompted. That is deliberately the least-harm reading, and it means a Sign in with Apple user who is 30 sees no nutrition targets. Repay when the owner answers the A16.b age-questionnaire gate: either accept it, or add one birth-year question at the point the target flow is first opened (never at launch, never retroactively).

- 2026-09-10 · A13 · Open (owner) · exercise art is ruled to be VENDORED from bryllim/workout-guide (artwork CC BY-SA 4.0, code MIT, from Everkinetic) rather than commissioned, which buys speed for two obligations the repo must carry forever: an attribution screen per CC BY-SA 4.0 §3(a)(1), and a hard rule that no vendored SVG is ever edited on disk (tinting happens at render time) because an edited file is Adapted Material and would pull the ShareAlike condition onto it. The second is enforced by a hash manifest test (W069) precisely because a future session cannot be trusted to remember it. Repay by commissioning original line art (~$4,000–8,000 for 110 figures) if the voice ever matters more than the schedule, or by moving to a no-ShareAlike source; either removes both obligations.

- 2026-09-10 · A14 / W041 · Open · C5's extraction of the sets×reps phrase is HALF done. The web's `targetsLabel` (GeneratedPlan.tsx) now delegates to the new `home-lines.ts` twin, because it was character-for-character the same string. The iOS side does NOT: `WorkoutDraftText.subtitle` renders `{sets} × {reps} · {Equipment}` with spaces around the × and the equipment appended, which is a different line for a different surface, so routing it through `HomeLines.setsByReps` would mean a second formatting parameter — abstraction the doctrine forbids for two callers (C1/C6). Repay if a third iOS caller of the spaced form appears: at that point the two forms are a real pair and `setsByReps` grows one concrete sibling rather than a flag.

- 2026-09-10 · A14 / W044 · Open · the web's bottom anchor is best-effort where the iOS one is exact. iOS uses `.frame(minHeight: proxy.size.height)` plus a `Spacer`, which is deterministic. The web uses `.stack--page { min-height: 100% }` with `.stack--bottom { margin-top: auto }`, and the percentage resolves only because `.app-column` is a flex child with `flex: 1` and therefore a definite height. If a future layout change gives `.app-column` an auto height the anchor silently degrades to "the group just follows the content" — which is the pre-A14 behaviour, so it fails safe, but it fails SILENTLY. A `:has()`-scoped flex column on `.app-column` would make it explicit; it was not used because C6 asks that a junior predict every line, and `:has()` in a shared stylesheet is a trap. Repay with a Playwright assertion that measures the vector row's distance from the viewport bottom on the phone viewport.

- 2026-09-10 · A14 / W045 · Open (by design, but it must stay visible) · the gamification engine's `PostKind` stays `workout | meal | text` while the post DOCUMENT now has a fourth type, `cardio`, and `gamification-store.ts` maps `cardio → workout` at the boundary. This is deliberate: it is what lets A14 ship without touching a single append-only vector, and a walk keeps earning +25 and sustaining the streak exactly as before. The cost is one mapping that looks like a redundancy to anyone reading `gamification-store.ts` alone — delete it and every cardio log silently stops earning XP, with no vector going red because no vector carries a cardio post. `web/tests/api/post-types.test.ts` exists to fail loudly in that case. Repay only if the engine ever needs to distinguish the two, and then it is a Decision Registry entry plus a new vector, not an edit.

- 2026-09-10 · A14 / H011 · Open · `missedGray` #A8A29A is used as a GRAPHICAL OBJECT in four places and measures 2.39:1 on the bone canvas and 2.53:1 on a card — both below 6.5's 3:1 non-text gate: the week strip's missed ring, the crew strip's not-posted dot, the unlit streak flame, and the heat map's neutral cells. The A14 pass certified these compliant by answering the WRONG RULE — the plan and both platforms argue "the marks carry a shape as well as a colour, so the strip reads in grayscale", which answers WCAG 1.4.1 (colour is not the only channel) and says nothing about 1.4.11 (non-text contrast), which is the gate the spec actually states. Stage C of `docs/home-plan-2026-09-10.md` repays the Home sites by pointing them at `secondaryText` (5.18:1 / 5.49:1) rather than changing the token. The heat map and the unlit flame are NOT covered by that stage and stay owed. Repay by darkening the `missedGray` token itself (~#8C857C), which is a Part III table value and needs owner ratification.


- 2026-09-10 · 8.9 / H011 · Open · **spec:1124's iOS snapshot matrix has never been built, and its absence was itself unrecorded.** The spec requires S07 Home, S02, S09, S10 and S12 rendered at {SE 375×667, Pro Max 440×956} × {default, accessibility-XXL} and diffed in CI; what exists is `ios/CrewUITests/Screenshots.swift`, ad-hoc attachments whose own header calls them "the first step toward" that matrix. `debt.md:11` promises the weight tape will be repaid "by the 8.9 snapshot matrix at SE/XXL" — a matrix that does not exist. 8.9 is a Phase 5 exit criterion, so the gate reads green on paper while being unmet. This is why a ~22% contiguous void and an XXL truncation shipped to TestFlight with every CI job green. Compounding it: `ci.yml`'s simulator selection sorts by number and takes the LAST, so CI always runs the newest and largest iPhone and has never once exercised the smallest layout. Stage I asserts layout PROPERTIES instead of pixels (baselines cannot be recorded without a Mac) and adds an SE-class device; the pixel matrix stays owed.

- 2026-09-10 · A14 / H011 · Repaid same day · the iOS and web Home spent different amounts of vertical slack at the same seam — iOS `Spacer(minLength: sectionGap)` inside a `VStack(spacing: sectionGap)` totalled a hard 72 pt floor (24 + 24 + 24) while the web twin spent 24 px, so the two platforms composed an identical rest day differently and no screenshot comparison could agree. H001 sets `minLength: 0`, leaving the two stack gaps on both. Recorded rather than silently fixed because the A14 pass logged the OPPOSITE direction (that the web anchor was the weaker one) and a future reader comparing the two needs to know which way the divergence actually ran.

- 2026-09-10 · A17.2 / H036 · Repaid same day, and worth recording because it was INVISIBLE · the web bottom anchor did nothing at all. `.stack--page` asked for the viewport's height with `min-height: 100%`, which resolves against the PARENT'S height property — and `.app-column`'s was `auto`, so it computed to `auto` and `margin-top: auto` on the bottom group had no free space to consume. The layout looked plausible in review, passed tsc, passed lint, passed every existing e2e assertion, and was wrong: the day's primary control sat at y=186 on a 667 px phone. It was found by the FIRST RUN of the new `tests/e2e/home-layout.spec.ts`, which is the entire argument for asserting layout properties rather than eyeballing them. `.app-column` is now a flex column and `.stack--page` is `flex: 1`. The iOS twin (`.frame(minHeight:)` + `Spacer`) was always deterministic; this was a web-only defect, so the platforms had silently diverged on the thing A14 said they agreed about.

- 2026-09-10 · 8.9 / H037 · Open (owner declined the scope, deliberately) · CI's simulator picker still sorts available iPhones by number and takes the LAST, so it always runs the newest and largest device and has never once exercised an SE-class layout. The owner chose "assert layout properties in code" over "add an SE device to CI" (D18), and the web half of that now exists (`tests/e2e/home-layout.spec.ts` asserts at 375×667: no horizontal scroll, no control label clipped by its own box, the day's controls in the lower half, no gap larger than a third of the screen). The iOS half has no equivalent: SwiftUI layout cannot run on this Windows machine or on the Linux engine target, so the only iOS layout evidence remains the journeys' screenshots on whatever simulator CI happened to pick. Repay with the real 8.9 matrix on a Mac, or by adding the SE to the picker if the owner reverses D18.

- 2026-09-10 · CI speed · Open (measured, deliberately declined) · **parallel UI testing is NOT enabled, and the reason is the timeouts, not a hunch.** `-parallel-testing-enabled YES -maximum-parallel-testing-workers 2` would clone the simulator and split the five journey classes (CameraDenied 44.0 s, Journey1 38.1 s, OfflineSession 35.5 s, Journey2 26.9 s, Launch 4.2 s) into ~75 s of makespan instead of 149 s — but a clone costs ~20–30 s to boot, so the real saving is ~45 s, and it is bought by running two simulators plus `next dev` on a **3-core** macOS runner. The journeys are full of 2- and 3-second waits (`waitForExistence(timeout: 2)` in Journey1, OfflineSession and CameraDenied); under that much CPU contention a 2 s wait is not a timeout, it is a coin flip. A flaky journey costs more than 45 s the first time it goes red for no reason. Revisit if GitHub's macOS runners get more cores, or if the tight waits are raised first.

- 2026-09-10 · 8.4 / CI speed · Open (owner's call — it trades coverage for time) · **two of the five journeys re-drive the whole onboarding flow through the UI purely as setup**, ~20 s each. `OfflineSessionTests` and `CameraDeniedTests` both tap hero → seven day toggles → Brand new → Full gym → Looks good → name/email/password/birth-year → Home, which is precisely what `Journey1_NewUserTests` exists to cover as its SUBJECT. Journey ② already shows the cheaper pattern: seed the member through the API with `SeedClient` and launch signed in via `-seededReturningUser` + `CREW_SEED_SESSION`. Converting the two would take ~40 s off the journeys (149 s → ~110 s) and remove ~40 UI interactions' worth of flake surface. It is NOT done unasked because it changes what those tests prove: OfflineSession's subject is that the Keychain carries a session across a kill, and today it proves that for a session written by a real signup rather than one injected at launch — the exact path that was broken in run 34367618719 (errSecMissingEntitlement, -34018). CameraDenied needs a plan whose single training day is not today, which `SeedClient` cannot express yet (`putPlanForEveryDay` is all it has). Repay by adding `putPlan(days:)` to SeedClient and converting both, IF the owner accepts the slightly weaker Keychain assertion.

- 2026-09-10 · CI speed · Open (not on the critical path today, will be tomorrow) · after this pass the two chains **balance**: `contracts → ios` is ~4¾ min and `contracts → web → web-e2e` is ~4m 37 s (web 1m 19 s, e2e 3m 10 s). Any further ios work alone therefore buys nothing in wall-clock — the next real saving has to come from the web chain, where `web-e2e` `needs: web` purely as a gate to avoid burning e2e minutes on code that fails lint. Both are ubuntu (no minutes multiplier), so running them concurrently would cut ~1m 20 s off total wall time at the cost of occasionally running e2e against code that would have failed `npm run lint`. Vitest also reports ~4.6 s recoverable in `web` via `isolate: false`, which reuses workers across files — NOT taken, because 41 files including the API suites share an in-memory Mongo and module-level state, and cross-file contamination is a much worse failure than 4.6 s.

- 2026-09-10 · CI speed · **Repaid same day, and the lesson is the entry** · `build-for-testing` + 2× `test-without-building` made the ios job 86% SLOWER (6m11s → 11m31s, run 34542854485) and is now warned against in `ios/project.yml` and `ci.yml`. The pass that introduced it was reasoned from a number that was never measured: "92 s build + 82 s build", obtained by subtracting test time from step time. An `xcodebuild test` step is THREE phases, not two — compile, simulator preparation (boot, install, automation session), tests — and the middle one is ~60 s that the subtraction silently handed to the compiler. xcodebuild reports it directly as `IDETestOperationsObserverDebug: N elapsed`; that counter is the one to read before touching this job again. The true duplicate compile was ~21 s. The true expensive duplicate was the preparation, at ~122 s, and splitting the build out did not remove it — it tripled each one and still paid it twice, because `test-without-building` has no build phase to overlap it with. **Generalisable rule: never infer a phase's cost by subtraction when the tool reports the phase directly.**

- 2026-09-10 · 8.4 / CI · Open (reduced, not closed) · the journeys' waits were tuned on a fast runner and three of them still are. Sixteen POSITIVE `waitForExistence` timeouts went 2–3 s → 15 s after CameraDenied blew a 2-second wait on a loaded machine; a positive wait returns as soon as the element appears, so that is free. The three NEGATIVE waits (`XCTAssertFalse(x.waitForExistence(timeout: 2))` in Journey ①, Journey ② and CameraDenied) cannot be raised the same way — they burn their whole timeout every time they PASS, so raising them adds 13 s per site to every green run. They are therefore still tuned to runner speed, and a fast-enough machine could in principle make one of them flake in the other direction (the element appears after the window and the assertion wrongly passes). The correct repair is to assert absence differently — poll for the element's disappearance with an explicit expectation, or assert on a positive marker of the new state instead — not to change the timeout. Same for `share.waitForExistence(timeout: 5) || done.waitForExistence(timeout: 1)` in Journey ②, where a solo member waits out the first branch before the second is ever checked.

- 2026-09-17 · A21.4 / 8.4 · Open (device-only by nature) · **the reminder opt-in's ACCEPT path is not exercised by any automated test.** "Remind me" asks the system permission, calls `registerForRemoteNotifications` and PATCHes `reminderTime`; the simulator has no APNs (registration fails silently, `PushRegistrar.didFailToRegister…` is a deliberate no-op), and the system alert's answer would come from the UI-interruption monitor's "quiet button", so the journeys (①, ②, HomeStates) answer "Not now". What IS covered: `ReminderOptin.shouldAsk` (asked once, after the first completed workout, while no time is stored), the token's hex shape, the 7:30 pre-fill (ReminderOptInTests), and the server side of the token (push-token route tests, `pushToken` sync op). Repay in the W7 device pass: tap "Remind me" on the phone, allow, and confirm a `pushTokens` row and `reminderTime` on `GET users/me`.

- 2026-09-17 · A21.9 / 5.3 · Open (a deliberate shape, recorded) · **a celebration is remembered OUTSIDE the Store.** "No post exists until a button is tapped" leaves a window — Complete tapped, the app killed under the celebration — in which the session is completed and no post exists on either side. The unanswered celebration (its post id and session id) is kept in UserDefaults (`SessionActions.unansweredKey`) rather than as a field on `LocalSession`, because adding a SwiftData attribute is a schema change and the store has no migration (the 2026-09-09 entry). The next cold start answers it PRIVATELY (`postUnanswered`, `HomeScreen.task`). Consequences: (a) the private default is silent — the person never learns the choice was made for them; (b) a reinstall loses the memory, and the server then holds a completed session with no post forever (the day never counted); the server-side repair — a cron that posts privately for completed sessions older than a day with no post — is NOT built (it needs an owner call on the default). Repay with W9's SwiftData VersionedSchema migration (a `pendingPostClientId` on the session) or the cron, whichever the owner picks.

- 2026-09-17 · A21.3 / S13 · Open (small, visible) · `InviteCodeEntry`'s "Paste" button reads `UIPasteboard.general.string`, which on iOS 16+ shows the system paste banner the first time and may ask "Allow Paste" on a device; the journeys type the code instead (the simulator's pasteboard is not the test's). If the banner proves annoying on the smoke test, replace the button with a `PasteButton(payloadType: String.self)` — the system control that avoids the prompt.

- 2026-09-17 · 8.7 / W5 · Open (verifiable only with the store) · **the private-blob read path has never run.** `storePhoto` now writes `access: "private"` and `readStoredPhoto` reads with `get(url, { access: "private", token })`; with no `BLOB_READ_WRITE_TOKEN` (this machine, CI, the beta so far) every photo takes the local substitute, which streams and is tested. The Blob store `crew-photos` is still not connected (the owner's W7 step), so no real blob exists yet — which also means no PUBLIC blob from before W5 survives to be cleaned up. Repay in W7's device pass: connect the store, post one photo from the phone, open it from a crew-mate's account (200 stream) and as a stranger (403), and try the raw blob URL in a browser (must be refused).

- 2026-09-17 · 1A / Flow 6 / W5 · Open (found while binding the Apple callback; not fixed — it changes the flow, not the security) · **a web Apple signup that started from an invite lands on /crew WITHOUT joining the crew.** The email path joins inside `OnboardingFlow.finish` (`joinCrew(draft.invite)`) and then routes to /crew; the Apple path leaves the page for Apple and returns through the callback's 303 to `next = /crew`, and nothing on that path has the invite token in hand — the pre-auth draft (with the invite) is in the browser's localStorage, untouched. The person sees the solo Crew tab with "I have an invite" (W4) and can paste the code again, so it is recoverable, not a dead end. Repair options: carry the invite in the start route's state (a fourth field, joined in the callback after sign-in) or have the Crew page read the pending draft's invite once on arrival. Owner's call which; the state field is the smaller change.

- 2026-09-17 · E9 / W5 · Open (deliberately not built) · **there is no server-side answer to a completed session that never got its post.** W4's `postUnanswered` repairs it from the phone that made it; a reinstall between Complete and the tap loses that memory and the server keeps a completed session with no post forever (the day never counts). The natural repair is a cron that posts privately for any completed session older than a day with no post — not built, because "silently private" as the server's default is the owner's call (see the A21.9 entry above). Repay when the owner rules; it is a ten-line addition to `api/cron/notifications` or its own cron.

- 2026-09-17 · A21.12 / W6 · Open (OWNER INPUT) · **the Home decision on A20 Build B is not taken.** A21.12 makes it the owner's call after the device walkthrough, and the walkthrough has not happened (no build past 126 has reached the phone — the certificate cap). `archive/a20-build-b` (e509314) stands untouched. When the owner rules rebuild-or-drop, record it as an Appendix A line and either port Build B's Home (a full session: the type scale, the card order) or delete the archive branch's claim on the future.

- 2026-09-17 · A21.10 / W6 · **Closed 2026-09-18 by the owner's ruling LIGHT ALWAYS** (Appendix A, amends A21.10): the dark half is retired — UIUserInterfaceStyle Light on iOS, color-scheme light and no dark stylesheet block on web; the light screenshots stay as the owner's review surface. The original entry, for the record: **the light-mode pass and dark check are photographs, not a verdict.** Nothing on this machine renders SwiftUI, so W6's evidence is `Journey4_ScreensTests`: every tab shot in light and in dark (`-AppleInterfaceStyle Dark`, the simulator's user-defaults override) into the xcresult, plus the code-level pass (no hard-coded colour under ios/Crew, every screen on the canvas token, every List's system background hidden). The owner reviews the screenshots; anything wrong there is a W6 follow-up, not a W6 miss. If the dark shots come out light, the override is being ignored by this simulator and the test should set `UIUserInterfaceStyle` through the app's `-uiTest` launch handling instead.

- 2026-09-17 · W6 · Open (needs the owner's screenshot) · **"Progress background token" changed nothing.** The walkthrough item names a token problem on Progress; the audit found none to change (no hard-coded colour, canvas on every screen, the heat map's empty cell already the hairline token after H014). Either the owner saw the pre-A19.4 pushed Journal screen (now gone) or a dark-mode rendering the screenshots will show. Repay from the owner's screenshot of what looked wrong.

- 2026-09-17 · A21.11 / W6 · Open (CI's verdict decides) · the compact rows and the hidden tab bar are new layout on the one screen the journeys drive hardest (Skip / Complete inside the window, the set row as a button). The journeys still target the ACTIVE exercise's card, which is unchanged; if run 1 of the push goes red on `expectOnScreen`, the compact row's `minHeight` or the hidden tab bar's safe-area change is the first suspect.

- 2026-09-18 · 8.4 / CI · Open (observed, not chased) · **two `CoreData: error: addPersistentStoreWithType … NSCocoaErrorDomain (512)` lines appear in every ios job's `xcodebuild test` step** — the same two in run 35302729856 (W3–W6) and run 35311124097 (build 150), so they predate tonight's changes and every test is green around them. Error 512 is NSFileWriteUnknownError on the simulator app's `default.store`; the likely source is a UI test's `-resetState` deleting rows while the SwiftData container is open, or F31's self-repair path opening a store the previous launch left behind. Repay by reading the xcresult around the two lines once (which test, which launch) — if it is F31 doing its job, say so here and close; if a store is being recreated on every launch, that is a launch-time cost worth removing.

- 2026-09-18 · A22 G1 (a) / engines · Open (until a ruling deletes the constants) · **the meal and text branches stay in both engines as fixture-only dead code.** `xpMealPost`, `mealXpDailyCap`, `PostKind.meal/.text`, `DayCounters.meals` and the `kind == meal` branch remain because the twenty-three retired vectors still shape-check and the surviving fixtures are frozen by the append-only rule; after item 2 (b)–(d) no client creates either kind. Removing them needs a ruling that also deletes the two constants (spec-constants are vector-bound).

- 2026-09-18 · A22 G1 (a) / recompute · Open (design limit) · **plan history is not kept, so a plan edit rewrites the past.** The server fold and iOS `judgeElapsedDays` judge an unposted past day by the CURRENT plan's weekdays (R-068 reading 1): moving Wednesday to Thursday today turns last week's Wednesday silence into a rest day and last week's Thursday into a miss on the next recompute. Bounded by the forward-only plan editor; a plan-history collection would close it and is not in scope (A21.13).

- 2026-09-18 · A22 G2 / iOS Store · Open (closes with W9's VersionedSchema) · **LocalPost keeps four legacy stored properties nothing writes.** `photoKey`, `localPhotoPath`, `mealTag` and `earlierToday` stay on the SwiftData model because removing a stored property is a schema change and the Store has no migration yet (F31 self-repair only); hydration and every writer set them nil/false. W9's migration drops them.

- 2026-09-18 · A24 / UI tour · Open (by design) · **a tour step never fails CI, so a broken tour is silent until someone reads CHANGES.md.** `tourTap` returns false and the flow moves on; a renamed button shows up only as screens under "Removed". Chosen because a red tour on master would hold every TestFlight build on a tooling flake (A24 (2)); the 8.4 journeys keep the assertions. The fetch hook puts the Removed list in front of every session.

- 2026-09-18 · A24 / UI tour · Open (no frozen clock in the spec) · **fourteen tour screens are "expected drift".** Home, the week map, Progress, the journal, the celebration, the rest timer, the pause screen and the invite sheet render a weekday, a date, an elapsed time or a random code, so their baselines go stale overnight; `ios/scripts/tour-diff.mjs` lists them by name and reports a difference there as drift, not as a change — which also means a REAL change on those screens is reported under the drift heading. Closing it needs an app clock the spec does not define.

- 2026-09-18 · A24 / CI · Open (cost) · **every push to any branch now starts `ci`** — one Linux minute (`contracts`) when no UI path moved, the ~10-minute macOS job when one did; an open PR still gets its own pull_request run as before, so a UI push to a PR branch pays macOS twice. The tour adds eleven UI tests (≈ 12 launches) to the ios job on master. Baselines are PNGs in git (~40 files); re-approving all of them adds their full weight to history each time.

- 2026-09-18 · W8a / fast-food seed · Open (owner releases a chain with one edit) · **the seed ships six of the ~ten proposed chains.** Chick-fil-A, Chipotle, Panera Bread, Starbucks, Subway and Wendy's were read from the chains' own publications on 2026-09-18 (15 items each); McDonald's, Burger King, Five Guys and Taco Bell are HELD in docs/fast-food-seed-sources.md with the reason each could not be read from its own current publication without a workaround. `fastFoodChainCount` says 6, so check-seeds asserts what actually shipped. Repay by moving a held chain's verified rows into `shared/seed/fast-food.json` and raising the constant (R-074 reading 10).

- 2026-09-18 · A22 / achievements · Open (observed, both twins agree) · **`workoutsCompleted` counts cardio logs.** The achievement counter is the number of completed sessions of any kind on the server (`achievement-facts.ts`) and on the phone (`AchievementFacts.swift`), so a logged walk earns "Showed up" before the first strength workout. Pre-dates A22; A22 made it visible because a walk no longer counts the streak day while it still counts here. Behaviour left as built (a cardio log IS a completed session, A8); a ruling that the counter means planned workouts only is a one-line filter on both sides plus a vector.

- 2026-09-18 · W8c / iOS · Open (CI's verdict decides) · **the nutrition screens are WRITTEN — UNVERIFIED like every app-target file**: eleven files under `Features/Nutrition`, four under `Storage`, one `Api` file, the Settings rows, and `Journey5_NutritionTests`. Only the engine half compiles on this machine (94 package tests green); swift-xref and doctrine-lint are clean; the macOS job is the compiler. First suspects if it goes red: the two-sheet hand-off in `SavedMealsScreen` (chain picker → meal form, presented from `onDismiss`), `GramField`'s text binding, and the `#Predicate { kinds.contains($0.kind) }` fetch in `NutritionLocal.queuedOps`.

- 2026-09-18 · W8c / Store · Open (closes with W9's VersionedSchema) · **four @Model classes joined the schema without a migration plan.** Adding entities is a lightweight change SwiftData performs on its own; if it ever does not, F31's self-repair starts the store over and the server refills it (nutrition included, the next time Nutrition opens). W9 writes the VersionedSchema that makes this explicit.

- 2026-09-18 · W8c / phone · Open (by design, R-074 (8)) · **"Delete my nutrition data" and the one-time birth year are network-only on the phone** — there is no op kind for either (the endPause precedent), so offline they fail and say so. Everything else in Nutrition works offline through the six queued ops.

- 2026-09-18 · W066 · Open (owner, on a device) · **the manual Grayscale-filter pass has not been done on a phone.** The five redundant encoders are built and journey ⑤ asserts the ink words, but "reads fully correctly with every macro token forced to plain ink" is a human look at Settings → Accessibility → Colour Filters → Grayscale on both Today screens. Listed in docs/OWNER-REVIEW.md.

- 2026-09-18 · A23 / copy · Open (owner ratifies) · **the education copy on screen is the owner's DRAFT.** Twelve whispers and the How Crew works page ship from `shared/copy/education.json` as drafted in docs/education-copy-draft.md, by the owner's order of 2026-09-18 (item 5); the note from Max is marked "Draft" on the page and the launch audit prints a NOTE for it. Repay by editing that one file on ratification (set `page.note.draft` to false with the rewritten note) — both platforms regenerate, and check-copy keeps the twelve-word rule.

- 2026-09-18 · A23 / iOS · Open (CI's verdict decides) · **one root-level simultaneous tap gesture clears whispers.** It is the smallest mechanism for "the first tap anywhere on that screen", but it sits over every screen in the app; if a journey goes red on a control that stopped answering taps, `RootView`'s `.simultaneousGesture` is the first suspect, and the fallback is a per-screen gesture on the nine screens that carry a whisper.
