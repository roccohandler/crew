# design/targets/

Approved mockups, one per screen, exported from Claude Design. Empty until the owner picks a direction.

Name a target after the tour shot it governs, without the step number: the tour's `03_plan_editor_filled.png` is judged against
`plan_editor_filled.png` here. `ui-reviewer` compares a screenshot with its target when one exists, and with design/DESIGN.md always.
